/* kill meeeeeeeeeeee */
#include <stdio.h>
#include <stdlib.h>

int main( )
{
	int input;
	fprintf(stdout, "===ael0===\n0. build list\n2. display list forward\n");
	fprintf(stdout, "3. display list backward\n3. insert into list\n");
	fprintf(stdout, "4. append into list\n5. obtain from list\n");
	fprintf(stdout, "6. clear list\n7. quit\n>>>");
	scanf("%d", &input);
	return (0);
}

